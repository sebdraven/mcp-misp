# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

bin/

# Default root for the out_dir tool parameter when it is pointed inside the repo.
out/
